-- ============================================================================
-- word_results 테이블에 discovered_by 컬럼 추가
-- ----------------------------------------------------------------------------
-- 목적: 해당 단어를 최초로 검색/생성한 사용자를 기록한다.
--       Google 계정의 사용자명(@ 앞 부분)을 저장.
-- ============================================================================

ALTER TABLE word_results
  ADD COLUMN discovered_by TEXT;
